# SafeLease AI - Core & Serverless API Dependencies (Vercel Ready)
# 경량화되어 Vercel Serverless Function 250MB 용량 한도 내에서 초고속으로 기동됩니다.

# 데이터 모델 및 설정
pydantic>=2.5.0
python-dotenv>=1.0.0

# 문서 파싱 (PDF, DOCX, 이미지)
pypdf>=4.0.0
python-docx>=1.1.0
pillow>=10.2.0

# CLI & 터미널 UI
rich>=13.7.0
typer>=0.9.0
click>=8.1.0

# AI / LLM (선택적 심층 추론)
google-genai>=0.1.0
